#:kivy 2.0.0

<MenuScreen>:
    BoxLayout:
        orientation: 'vertical'
        size: root.width, root.height
        padding: 20
        spacing: 20

        Label:
            text: 'Приложение'
            text_size: self.size
            halign: 'center'
            valign: 'middle'
            font_size: 30
            color: (0, 0, 0, 1)

        RoundedButton:
            text: 'Начать'
            font_size: 25
            on_press: app.root.current = 'main'

<MainScreen>:
    layout: layout.__self__
    BoxLayout:
        id: layout
        orientation: 'vertical'
        label: label.__self__
        size: root.width, root.height
        padding: 20
        spacing: 20

        Label:
            id: label
            text_size: self.size
            halign: 'center'
            valign: 'middle'
            text: 'Стоя у гимнастической стенки принять правильную осанку'
            font_size: 25
            color: (0, 0, 0, 1)

        BoxLayout:
            spacing: 20

            RoundedButton:
                text: '<--'
                font_size: 45
                pos_hint: {'center_x': 0.5}
                on_press: root.change_ex_prev(label, None)

            RoundedButton:
                text: '-->'
                font_size: 45
                pos_hint: {'center_x': 0.5}
                on_press: root.change_ex_next(label, None)

<EndingScreen>:
    BoxLayout:
        orientation: 'vertical'
        size: root.width, root.height
        padding: 20
        spacing: 20

        Label:
            text: 'Поздравляю'
            text_size: self.size
            halign: 'center'
            valign: 'middle'
            font_size: 35
            color: (0, 0, 0, 1)

        RoundedButton:
            text: 'Закрыть'
            font_size: 25
            on_press: app.stop()

<RoundedButton@Button>
    background_color: (0, 0, 0, 0)
    size_hint: (1, .2)
    color: (1, 1, 1, 1)
    background_normal: ''

    canvas.before:
        Color:
            rgba: (48/225, 84/225, 150/225, 1)

        RoundedRectangle:
            size: self.size
            pos: self.pos
            radius: [50]